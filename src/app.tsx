import { TicketForm } from "@/components/ticket-form";
import { DefaultLayout as Layout } from "@/layouts/default";

export function App() {
  return (
    <Layout>
      <TicketForm />
    </Layout>
  );
}
